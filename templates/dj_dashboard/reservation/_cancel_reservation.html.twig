<form method="post" action="{{ path('dashboard_dj_cancel_reservation', {'id': reservation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir vous retirer de cet évènement ?');">
        <div class="w-100 px-2 mb-2">
            <button class="btn btn-outline-danger text-nowrap w-100">
                <i class="bi bi-x"></i> Annuler
            </button>
        </div>
</form>
