<form method="post" action="{{ path('app_admin_team_delete', {'id': team_member.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_member.id) }}">
    <button class="btn">Delete</button>
</form>
