{% extends 'base.html.twig' %}

{% block title %}Production{% endblock %}

{% block body %}
    <h1>Production</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ production.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ production.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ production.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ production.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_production_index') }}">back to list</a>

    <a href="{{ path('app_admin_production_edit', {'id': production.id}) }}">edit</a>

    {{ include('admin_production/_delete_form.html.twig') }}
{% endblock %}
