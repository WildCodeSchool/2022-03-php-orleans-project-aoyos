<section class="partners my-5">
	<div class="container-fluid pt-3">
		<div class="row justify-content-center">
			{% if partners is not empty %}
				{% for partner in partners %}
					<img src="{{ asset('uploads/images/partner/' ~ partner.logo) }}" class="col-lg-2 partner-image mt-3"/>
				{% endfor %}
			{% else %}
				<h2 class="h3">Partenaires à venir !</h2>
			{% endif %}
		</div>
	</div>
</section>