{% for category, messages in app.flashes %}
    {% for message in messages %}
        <div class="text-center alert alert-{{ category }} mb-0">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}