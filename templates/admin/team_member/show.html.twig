{% extends 'base.html.twig' %}

{% block title %}TeamMember{% endblock %}

{% block body %}
    <h1>TeamMember</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team_member.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ team_member.name }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ team_member.position }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ team_member.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_team_index') }}">back to list</a>

    <a href="{{ path('admin_team_edit', {'id': team_member.id}) }}">edit</a>

    {{ include('admin/team_member/_delete_form.html.twig') }}
{% endblock %}
