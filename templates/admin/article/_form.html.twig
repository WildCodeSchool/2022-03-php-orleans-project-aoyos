<div class="d-flex justify-content-end">
	<a href="{{ path('admin_article_index') }}" class="btn btn-link my-3 pb-0 px-3">
		<i class="bi bi-arrow-left-square"></i>
		Retour
	</a>
</div>
{{ form_start(form) }}
{{ form_widget(form) }}

<button class="btn btn-outline-primary pb-0 px-3 mb-5">
	{{ icon|raw }}
	{{ button_label|default('Ajouter') }}
</button>

{{ form_end(form) }}
