{% extends 'admin/admin_base.html.twig' %}

{% block title %}MusicalStyle{% endblock %}

{% block body %}
    <h1>MusicalStyle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ musical_style.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ musical_style.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_musical_style_index') }}">back to list</a>

    <a href="{{ path('admin_musical_style_edit', {'id': musical_style.id}) }}">edit</a>

    {{ include('admin/musical_style/_delete_form.html.twig') }}
{% endblock %}
