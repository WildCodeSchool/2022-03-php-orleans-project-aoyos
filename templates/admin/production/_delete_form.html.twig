<form method="post" action="{{ path('app_admin_production_delete', {'id': production.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ production.id) }}">
    <button class="btn">Delete</button>
</form>
